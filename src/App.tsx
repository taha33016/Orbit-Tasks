import { useEffect, useMemo, useState } from 'react'
import { Activity, CalendarDays, Check, ChevronLeft, ChevronRight, FolderKanban, GripVertical, LayoutDashboard, Lightbulb, Plus, Save, Star, Trash2, UserCircle2, X } from 'lucide-react'

type Status='todo'|'doing'|'done'; type Role='user'|'manager'; type Person={id:string;name:string;specialty:string;age:number;role:Role}; type Group={id:string;name:string}; type Project={id:string;name:string;start:string;end:string;groupId:string;ownerId:string;description:string;sourceIdeaId?:string}; type Task={id:string;title:string;projectId:string;status:Status;groupId:string;assigneeId:string;createdAt:string}; type History={id:string;personId:string;action:string;taskId:string;projectId:string;at:string}; type Idea={id:string;title:string;description:string;groupId:string;authorId:string;createdAt:string;ratings:Record<string,number>;promotedProjectId?:string}
const people0:Person[]=[{id:'p1',name:'احمد سلیمانی',specialty:'توسعه‌دهنده',age:29,role:'manager'},{id:'p2',name:'محمد رضایی',specialty:'برنامه‌نویس',age:27,role:'user'},{id:'p3',name:'سارا احمدی',specialty:'طراح UI/UX',age:26,role:'user'}]; const groups0:Group[]=[{id:'g1',name:'توسعه'},{id:'g2',name:'طراحی'},{id:'g3',name:'بازاریابی'}]; const projects0:Project[]=[{id:'pr1',name:'وب‌سایت شرکت',start:'2026-09-01',end:'2026-09-24',groupId:'g1',ownerId:'p1',description:'طراحی و توسعه وب‌سایت اصلی شرکت.'},{id:'pr2',name:'اپلیکیشن موبایل',start:'2026-09-10',end:'2026-10-18',groupId:'g1',ownerId:'p2',description:'نسخه اولیه اپلیکیشن.'},{id:'pr3',name:'کمپین پاییزه',start:'2026-09-20',end:'2026-10-08',groupId:'g3',ownerId:'p3',description:'برنامه‌ریزی کمپین پاییزه.'}]; const tasks0:Task[]=[{id:'t1',title:'طراحی صفحه اصلی',projectId:'pr1',status:'done',groupId:'g2',assigneeId:'p3',createdAt:'2026-09-01'},{id:'t2',title:'پیاده‌سازی داشبورد',projectId:'pr1',status:'doing',groupId:'g1',assigneeId:'p1',createdAt:'2026-09-04'},{id:'t3',title:'اتصال API',projectId:'pr1',status:'todo',groupId:'g1',assigneeId:'p2',createdAt:'2026-09-08'},{id:'t4',title:'طراحی رابط کاربری',projectId:'pr2',status:'doing',groupId:'g2',assigneeId:'p3',createdAt:'2026-09-10'},{id:'t5',title:'ساخت نسخه اولیه',projectId:'pr2',status:'todo',groupId:'g1',assigneeId:'p2',createdAt:'2026-09-11'},{id:'t6',title:'تقویم محتوایی',projectId:'pr3',status:'todo',groupId:'g3',assigneeId:'p3',createdAt:'2026-09-20'}]; const meta:Record<Status,{title:string;dot:string;weight:number}>={todo:{title:'در انتظار انجام',dot:'bg-slate-400',weight:0},doing:{title:'در حال انجام',dot:'bg-amber-400',weight:50},done:{title:'انجام شده',dot:'bg-emerald-400',weight:100}}; const uid=(p:string)=>`${p}-${Date.now()}-${Math.random().toString(36).slice(2,8)}`; const iso=(d:Date)=>d.toISOString().slice(0,10); const jp=(d:Date)=>{const a=new Intl.DateTimeFormat('en-US-u-ca-persian',{year:'numeric',month:'numeric',day:'numeric'}).formatToParts(d);return{y:+a.find(x=>x.type==='year')!.value,m:+a.find(x=>x.type==='month')!.value,d:+a.find(x=>x.type==='day')!.value}}; const jf=(s:string)=>new Date(`${s}T00:00:00`).toLocaleDateString('fa-IR-u-ca-persian',{year:'numeric',month:'long',day:'numeric'}); const jn=(m:number)=>['فروردین','اردیبهشت','خرداد','تیر','مرداد','شهریور','مهر','آبان','آذر','دی','بهمن','اسفند'][m-1]; const dim=(y:number,m:number)=>m<=6?31:m<=11?30:((y%33===1||y%33===5||y%33===9||y%33===13||y%33===17||y%33===22||y%33===26||y%33===30)?30:29); const score=(i:Idea)=>{const a=Object.values(i.ratings);return a.length?a.reduce((x,y)=>x+y,0)/a.length:0}; const prog=(id:string,t:Task[])=>{const a=t.filter(x=>x.projectId===id);return a.length?Math.round(a.reduce((s,x)=>s+meta[x.status].weight,0)/a.length):0}
export default function App(){const[page,setPage]=useState<'board'|'timeline'|'ideas'|'activity'>('board'),[projects,setProjects]=useState(projects0),[tasks,setTasks]=useState(tasks0),[groups,setGroups]=useState(groups0); const[people,setPeople]=useState<Person[]>(()=>JSON.parse(localStorage.getItem('orbit-people')||'null')||people0),[ideas,setIdeas]=useState<Idea[]>(()=>JSON.parse(localStorage.getItem('orbit-ideas')||'[]')),[history,setHistory]=useState<History[]>([]),[drag,setDrag]=useState<string|null>(null); const[userId]=useState(()=>localStorage.getItem('orbit-current-user')||'p1'),[modal,setModal]=useState<'idea'|'project'|'task'|'group'|'account'|null>(null),[cal,setCal]=useState(()=>jp(new Date())),[deleteId,setDeleteId]=useState<string|null>(null); const current=people.find(p=>p.id===userId)||people[0]; const pg=useMemo(()=>Object.fromEntries(projects.map(p=>[p.id,prog(p.id,tasks)])),[projects,tasks]); const pm=Object.fromEntries(people.map(p=>[p.id,p])),gm=Object.fromEntries(groups.map(g=>[g.id,g])); useEffect(()=>localStorage.setItem('orbit-ideas',JSON.stringify(ideas)),[ideas]); useEffect(()=>{const today=iso(new Date()),a=jp(new Date(`${today}T00:00:00`)); if(!localStorage.getItem('orbit-idea-promotion-'+a.y+'-'+a.m)){const prev=a.m===1?`${a.y-1}-12`:`${a.y}-${String(a.m-1).padStart(2,'0')}`;const w=[...ideas].filter(i=>{const x=jp(new Date(`${i.createdAt}T00:00:00`));return `${x.y}-${String(x.m).padStart(2,'0')}`===prev&&!i.promotedProjectId}).sort((x,y)=>score(y)-score(x))[0];if(w){const p:Project={id:uid('pr'),name:w.title+' — ایده برتر ماه',start:today,end:iso(new Date(Date.now()+30*86400000)),groupId:w.groupId,ownerId:w.authorId,description:`ایده ثبت‌شده توسط ${pm[w.authorId]?.name||'عضو تیم'}\n\n${w.description}`,sourceIdeaId:w.id};setProjects(x=>[...x,p]);setIdeas(x=>x.map(i=>i.id===w.id?{...i,promotedProjectId:p.id}:i))}localStorage.setItem('orbit-idea-promotion-'+a.y+'-'+a.m,'done')}},[ideas,pm]); const move=(id:string,s:Status)=>{const t=tasks.find(x=>x.id===id);if(!t)return;setTasks(x=>x.map(a=>a.id===id?{...a,status:s}:a));setHistory(x=>[{id:uid('h'),personId:current.id,action:meta[s].title,taskId:id,projectId:t.projectId,at:new Date().toISOString()},...x]);setDrag(null)}; const addIdea=(title:string,description:string,groupId:string)=>{if(title.trim())setIdeas(x=>[{id:uid('idea'),title:title.trim(),description:description.trim(),groupId,authorId:current.id,createdAt:iso(new Date()),ratings:{}},...x]);setModal(null)}; const rate=(id:string,v:number)=>setIdeas(x=>x.map(i=>i.id===id?{...i,ratings:{...i.ratings,[current.id]:v}}:i)); const addProject=(name:string,start:string,end:string,groupId:string,description:string)=>{if(name.trim())setProjects(x=>[...x,{id:uid('pr'),name:name.trim(),start,end,groupId,ownerId:current.id,description}]);setModal(null)}; const addTask=(title:string,p:string,g:string,a:string)=>{if(title.trim())setTasks(x=>[...x,{id:uid('t'),title:title.trim(),projectId:p,status:'todo',groupId:g,assigneeId:a,createdAt:iso(new Date())}]);setModal(null)}; const del=(id:string)=>{if(current.role!=='manager')return;setProjects(x=>x.filter(p=>p.id!==id));setTasks(x=>x.filter(t=>t.projectId!==id));setHistory(x=>x.filter(h=>h.projectId!==id));setDeleteId(null)}; const overall=projects.length?Math.round(projects.reduce((s,p)=>s+pg[p.id],0)/projects.length):0; return <div dir="rtl" className="min-h-screen bg-[#08090d] text-slate-200"><aside className="fixed right-0 top-0 bottom-0 hidden md:flex w-64 flex-col border-l border-white/10 bg-[#0d0f15] p-5"><div className="mb-8 flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-gradient-to-br from-violet-500 to-cyan-400 font-black">O</div><div><b className="text-white">Orbit Tasks</b><p className="text-xs text-slate-500">فضای کاری خلاق</p></div></div><nav className="space-y-2">{([['board','برد پروژه',LayoutDashboard],['timeline','تقویم شمسی',CalendarDays],['ideas','اتاق ایده‌ها',Lightbulb],['activity','فعالیت افراد',Activity]] as const).map(([id,l,I])=><button key={id} onClick={()=>setPage(id)} className={`w-full flex gap-3 items-center rounded-xl px-4 py-3 text-sm ${page===id?'bg-violet-500/15 text-violet-300':'text-slate-400'}`}><I size={18}/>{l}</button>)}</nav><div className="mt-auto rounded-2xl border border-white/10 p-4"><p className="text-xs text-slate-500">پیشرفت کل</p><div className="mt-2 h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-violet-500" style={{width:`${overall}%`}}/></div><b className="mt-2 block">{overall}٪</b></div></aside><main className="md:mr-64 p-5 md:p-8"><header className="mb-7 flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm text-violet-400">فضای کاری تیم</p><h1 className="text-3xl font-black text-white">{page==='ideas'?'اتاق ایده‌ها':page==='timeline'?'تقویم پروژه‌ها':page==='activity'?'فعالیت افراد':'مدیریت پروژه'}</h1></div><div className="flex flex-wrap gap-2"><button onClick={()=>setModal('account')} className="btn-secondary"><UserCircle2 size={18}/>{current.name}</button><button onClick={()=>setModal('idea')} className="btn-primary"><Lightbulb size={16}/>ثبت ایده</button><button onClick={()=>setModal('task')} className="btn-secondary"><Plus size={16}/>تسک</button><button onClick={()=>setModal('project')} className="btn-primary"><FolderKanban size={16}/>پروژه</button></div></header>{page==='ideas'&&<Ideas ideas={ideas} people={people} groups={groups} current={current.id} rate={rate} add={()=>setModal('idea')}/>} {page==='timeline'&&<Timeline projects={projects} progress={pg} cal={cal} setCal={setCal}/>} {page==='activity'&&<ActivityLog history={history} tasks={tasks} projects={projects} people={people}/>} {page==='board'&&<Board projects={projects} tasks={tasks} people={people} groups={groups} progress={pg} drag={drag} setDrag={setDrag} move={move} del={setDeleteId}/>} </main>{modal==='idea'&&<Modal title="ثبت ایده جدید" close={()=>setModal(null)}><IdeaForm groups={groups} submit={addIdea}/></Modal>}{modal==='project'&&<Modal title="پروژه جدید" close={()=>setModal(null)}><ProjectForm groups={groups} submit={addProject}/></Modal>}{modal==='task'&&<Modal title="تسک جدید" close={()=>setModal(null)}><TaskForm projects={projects} groups={groups} people={people} submit={addTask}/></Modal>}{modal==='account'&&<Modal title="حساب کاربری" close={()=>setModal(null)}><AccountForm person={current} submit={(n,s,a,r)=>{setPeople(x=>x.map(p=>p.id===current.id?{...p,name:n,specialty:s,age:a,role:r}:p));setModal(null)}}/></Modal>}{deleteId&&<Modal title="حذف پروژه" close={()=>setDeleteId(null)}><button onClick={()=>del(deleteId)} className="w-full rounded-xl bg-red-600 p-3 font-bold">حذف پروژه</button></Modal>}<style>{`.btn-primary,.btn-secondary{display:flex;align-items:center;justify-content:center;gap:7px;border-radius:12px;padding:10px 14px;font-size:13px;font-weight:700}.btn-primary{background:#7c3aed;color:white}.btn-secondary{border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.04);color:#cbd5e1}.chip{display:inline-flex;border-radius:999px;background:rgba(255,255,255,.06);padding:4px 8px;font-size:10px;color:#94a3b8}.field{width:100%;border:1px solid rgba(255,255,255,.1);background:#0b0d12;color:#e2e8f0;border-radius:10px;padding:10px;outline:none}`}</style></div>}
function Ideas({ideas,people,groups,current,rate,add}:{ideas:Idea[];people:Person[];groups:Group[];current:string;rate:(id:string,v:number)=>void;add:()=>void}){const pm=Object.fromEntries(people.map(p=>[p.id,p])),gm=Object.fromEntries(groups.map(g=>[g.id,g]));const list=[...ideas].sort((a,b)=>score(b)-score(a));return <section><div className="mb-5 rounded-2xl border border-violet-500/20 bg-violet-500/10 p-5 flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-black text-white">ایده‌های تیم</h2><p className="mt-1 text-sm text-slate-400">هر عضو می‌تواند ایده ثبت کند و همه اعضا می‌توانند امتیاز بدهند.</p></div><button onClick={add} className="btn-primary"><Plus size={18}/>افزودن ایده جدید</button></div>{list.length===0?<div className="rounded-2xl border border-white/10 bg-[#10121a] p-14 text-center"><Lightbulb className="mx-auto text-violet-400"/><p className="mt-3 text-slate-400">هنوز ایده‌ای ثبت نشده است.</p><button onClick={add} className="btn-primary mx-auto mt-4">ثبت اولین ایده</button></div>:<div className="grid grid-cols-1 lg:grid-cols-2 gap-4">{list.map((i,n)=><article key={i.id} className="rounded-2xl border border-white/10 bg-[#10121a] p-5"><div className="flex justify-between"><div><span className="text-xs text-violet-300">رتبه {n+1}</span><h3 className="mt-1 text-lg font-black text-white">{i.title}</h3></div><div className="text-left"><b className="text-xl text-yellow-300">{score(i).toFixed(1)}</b><p className="text-[10px] text-slate-600">{Object.keys(i.ratings).length} رأی</p></div></div><p className="mt-3 text-sm leading-7 text-slate-400">{i.description||'بدون توضیحات'}</p><div className="mt-4 flex flex-wrap gap-2"><span className="chip">گروه: {gm[i.groupId]?.name}</span><span className="chip">ایده‌دهنده: <b className="mr-1 text-slate-200">{pm[i.authorId]?.name}</b></span><span className="chip">{jf(i.createdAt)}</span></div>{i.promotedProjectId?<div className="mt-4 text-xs text-emerald-300">✓ به پروژه تبدیل شده است.</div>:<div className="mt-4"><p className="mb-2 text-xs text-slate-500">امتیاز شما</p><div className="flex gap-1">{[1,2,3,4,5].map(v=><button key={v} onClick={()=>rate(i.id,v)} className={`rounded-lg p-2 ${i.ratings[current]===v?'bg-yellow-400/20 text-yellow-300':'bg-white/5 text-slate-500'}`}><Star size={18} fill={i.ratings[current]===v?'currentColor':'none'}/></button>)}</div></div>}</article>)}</div>}</section>}
function Timeline({projects,progress,cal,setCal}:{projects:Project[];progress:Record<string,number>;cal:{y:number;m:number;d:number};setCal:(x:{y:number;m:number;d:number})=>void}){const days=dim(cal.y,cal.m);const find=(target:number)=>{for(let i=0;i<370;i++){const d=new Date(Date.UTC(2026,0,1+i));const p=jp(d);if(p.y===cal.y&&p.m===cal.m&&p.d===target)return d}return new Date()};const first=find(1),offset=(first.getUTCDay()+1)%7,cells=Array.from({length:offset+days},(_,i)=>i<offset?null:i-offset+1);const dayIso=(n:number)=>{const d=new Date(first);d.setUTCDate(d.getUTCDate()+n-1);return iso(d)};const active=(p:Project,s:string)=>p.start<=s&&s<=p.end;const change=(n:number)=>{let y=cal.y,m=cal.m+n;if(m>12){m=1;y++}if(m<1){m=12;y--}setCal({y,m,d:1})};return <section><div className="mb-5 rounded-2xl border border-white/10 bg-[#0f1118] p-5 flex items-center justify-between"><button onClick={()=>change(-1)} className="btn-secondary"><ChevronRight size={17}/>ماه قبل</button><div className="text-center"><h2 className="text-xl font-black text-white">{jn(cal.m)} {cal.y}</h2><p className="text-xs text-slate-500 mt-1">شروع و پایان هر پروژه روی تقویم مشخص است.</p></div><button onClick={()=>change(1)} className="btn-secondary">ماه بعد<ChevronLeft size={17}/></button></div><div className="rounded-2xl border border-white/10 bg-[#0f1118] p-3 overflow-x-auto"><div className="min-w-[900px]"><div className="grid grid-cols-7">{['شنبه','یکشنبه','دوشنبه','سه‌شنبه','چهارشنبه','پنجشنبه','جمعه'].map(x=><div key={x} className="p-2 text-center text-xs text-slate-500">{x}</div>)}</div><div className="grid grid-cols-7 gap-1">{cells.map((d,i)=>{const s=d?dayIso(d):'';const items=d?projects.filter(p=>active(p,s)):[];return <div key={i} className="min-h-[145px] rounded-xl border border-white/5 bg-white/[.015] p-2">{d&&<><b className="text-xs text-slate-300">{d}</b><div className="mt-2 space-y-1">{items.map(p=><div key={p.id} className={`rounded-md px-2 py-1.5 text-[10px] ${p.start===s?'bg-cyan-500/20 text-cyan-200':p.end===s?'bg-emerald-500/20 text-emerald-200':'bg-violet-500/15 text-violet-200'}`}><div className="truncate font-semibold">{p.name}</div><div className="text-[9px] mt-0.5 opacity-70">{p.start===s?'شروع پروژه':p.end===s?'پایان پروژه':'در حال اجرا'} · {progress[p.id]}٪</div></div>)}</div></>}</div>})}</div></div></div><div className="mt-5"><h3 className="mb-3 font-bold text-white">وضعیت و برنامه پروژه‌ها</h3><div className="grid grid-cols-1 lg:grid-cols-2 gap-3">{projects.map(p=><div key={p.id} className="rounded-xl border border-white/10 bg-[#10121a] p-4"><div className="flex justify-between gap-3"><div><b className="text-white">{p.name}</b><p className="mt-1 text-xs text-slate-500">{jf(p.start)} تا {jf(p.end)}</p></div><b className="text-violet-300">{progress[p.id]}٪</b></div><div className="mt-3 h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-violet-500" style={{width:`${progress[p.id]}%`}}/></div><p className="mt-2 text-xs text-slate-500">{p.start<=iso(new Date())&&p.end>=iso(new Date())?'● در حال انجام':'○ برنامه‌ریزی‌شده'}</p></div>)}</div></div></section>}
function Board({projects,tasks,people,groups,progress,drag,setDrag,move,del}:{projects:Project[];tasks:Task[];people:Person[];groups:Group[];progress:Record<string,number>;drag:string|null;setDrag:(x:string|null)=>void;move:(id:string,s:Status)=>void;del:(id:string)=>void}){const pm=Object.fromEntries(projects.map(p=>[p.id,p])),gm=Object.fromEntries(groups.map(g=>[g.id,g])),um=Object.fromEntries(people.map(p=>[p.id,p]));return <section><div className="mb-6 grid grid-cols-1 md:grid-cols-3 gap-3">{projects.map(p=><div key={p.id} className="rounded-2xl border border-white/10 bg-[#10121a] p-4"><div className="flex justify-between"><b className="text-white">{p.name}</b><button onClick={()=>del(p.id)} className="text-slate-600 hover:text-red-400"><Trash2 size={16}/></button></div><p className="mt-2 text-xs text-slate-500">{jf(p.start)} تا {jf(p.end)} · {progress[p.id]}٪</p><div className="mt-2 h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-violet-500" style={{width:`${progress[p.id]}%`}}/></div></div>)}</div><div className="grid grid-cols-1 lg:grid-cols-3 gap-5">{(Object.keys(meta) as Status[]).map(s=><div key={s} onDragOver={e=>e.preventDefault()} onDrop={()=>drag&&move(drag,s)} className="min-h-[400px] rounded-2xl border border-white/10 bg-[#0f1118] p-4"><div className="mb-4 flex items-center gap-2"><span className={`h-2.5 w-2.5 rounded-full ${meta[s].dot}`}/><b>{meta[s].title}</b></div>{tasks.filter(t=>t.status===s).map(t=><article key={t.id} draggable onDragStart={()=>setDrag(t.id)} className="mb-3 rounded-xl border border-white/10 bg-[#171923] p-4 cursor-grab"><GripVertical size={16} className="inline text-slate-600"/><b className="mr-2 text-white">{t.title}</b><p className="mt-2 text-xs text-slate-500">{pm[t.projectId]?.name} · {gm[t.groupId]?.name} · {um[t.assigneeId]?.name}</p></article>)}</div>)}</div></section>}
function ActivityLog({history,tasks,projects,people}:{history:History[];tasks:Task[];projects:Project[];people:Person[]}){const tm=Object.fromEntries(tasks.map(t=>[t.id,t])),pm=Object.fromEntries(projects.map(p=>[p.id,p])),um=Object.fromEntries(people.map(p=>[p.id,p]));return <section className="rounded-2xl border border-white/10 bg-[#0f1118] p-5"><h2 className="text-xl font-bold text-white">فعالیت افراد</h2>{history.length?history.map(h=><div key={h.id} className="mt-3 rounded-xl border border-white/5 p-4"><b>{um[h.personId]?.name}</b> تسک <b>{tm[h.taskId]?.title}</b> را به <span className="text-violet-300">{h.action}</span> برد. <p className="text-xs text-slate-600 mt-1">{pm[h.projectId]?.name} · {new Date(h.at).toLocaleString('fa-IR')}</p></div>):<p className="p-10 text-center text-slate-500">هنوز فعالیتی ثبت نشده است.</p>}</section>}
function Modal({title,close,children}:{title:string;close:()=>void;children:React.ReactNode}){return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4"><div className="w-full max-w-md rounded-2xl border border-white/10 bg-[#11131b] p-5"><div className="mb-5 flex justify-between"><h2 className="font-bold text-white">{title}</h2><button onClick={close}><X size={20}/></button></div>{children}</div></div>}
function IdeaForm({groups,submit}:{groups:Group[];submit:(t:string,d:string,g:string)=>void}){const[t,setT]=useState(''),[d,setD]=useState(''),[g,setG]=useState(groups[0]?.id||'');return <form onSubmit={e=>{e.preventDefault();submit(t,d,g)}} className="space-y-3"><input autoFocus className="field" placeholder="عنوان ایده" value={t} onChange={e=>setT(e.target.value)}/><textarea className="field resize-none" rows={6} placeholder="ایده خود را کامل توضیح دهید..." value={d} onChange={e=>setD(e.target.value)}/><select className="field" value={g} onChange={e=>setG(e.target.value)}>{groups.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><button className="btn-primary w-full"><Lightbulb size={16}/>ثبت ایده</button></form>}
function ProjectForm({groups,submit}:{groups:Group[];submit:(n:string,s:string,e:string,g:string,d:string)=>void}){const[n,setN]=useState(''),[s,setS]=useState(iso(new Date())),[e,setE]=useState(iso(new Date(Date.now()+30*86400000))),[g,setG]=useState(groups[0]?.id||''),[d,setD]=useState('');return <form onSubmit={x=>{x.preventDefault();submit(n,s,e,g,d)}} className="space-y-3"><input className="field" placeholder="نام پروژه" value={n} onChange={x=>setN(x.target.value)}/><input type="date" className="field" value={s} onChange={x=>setS(x.target.value)}/><input type="date" className="field" value={e} onChange={x=>setE(x.target.value)}/><select className="field" value={g} onChange={x=>setG(x.target.value)}>{groups.map(q=><option key={q.id} value={q.id}>{q.name}</option>)}</select><textarea className="field" rows={4} placeholder="توضیحات" value={d} onChange={x=>setD(x.target.value)}/><button className="btn-primary w-full">ساخت پروژه</button></form>}
function TaskForm({projects,groups,people,submit}:{projects:Project[];groups:Group[];people:Person[];submit:(t:string,p:string,g:string,a:string)=>void}){const[t,setT]=useState(''),[p,setP]=useState(projects[0]?.id||''),[g,setG]=useState(groups[0]?.id||''),[a,setA]=useState(people[0]?.id||'');return <form onSubmit={e=>{e.preventDefault();submit(t,p,g,a)}} className="space-y-3"><input className="field" placeholder="عنوان تسک" value={t} onChange={e=>setT(e.target.value)}/><select className="field" value={p} onChange={e=>setP(e.target.value)}>{projects.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select className="field" value={g} onChange={e=>setG(e.target.value)}>{groups.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select className="field" value={a} onChange={e=>setA(e.target.value)}>{people.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><button className="btn-primary w-full">ساخت تسک</button></form>}
function AccountForm({person,submit}:{person:Person;submit:(n:string,s:string,a:number,r:Role)=>void}){const[n,setN]=useState(person.name),[s,setS]=useState(person.specialty),[a,setA]=useState(String(person.age)),[r,setR]=useState<Role>(person.role);return <form onSubmit={e=>{e.preventDefault();submit(n,s,+a,r)}} className="space-y-3"><input className="field" value={n} onChange={e=>setN(e.target.value)}/><input className="field" value={s} onChange={e=>setS(e.target.value)}/><input className="field" type="number" value={a} onChange={e=>setA(e.target.value)}/><div className="grid grid-cols-2 gap-2"><button type="button" onClick={()=>setR('user')} className="btn-secondary">کاربر</button><button type="button" onClick={()=>setR('manager')} className="btn-secondary">مدیر</button></div><button className="btn-primary w-full"><Save size={16}/>ذخیره</button></form>}
